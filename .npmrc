# React Native's tooling (Metro, native autolinking) assumes a flatter
# node_modules than pnpm's default symlinked layout. Hoisting avoids most
# monorepo+Expo resolution issues; see apps/mobile/metro.config.js for the
# rest of what's needed on top of this.
node-linker=hoisted
