name = "security_reviewer"
description = "Read-only OWASP MAS reviewer for mobile storage, auth, crypto, network, platform, code, and resilience."
sandbox_mode = "read-only"
model_reasoning_effort = "high"
developer_instructions = """
Read .agents/roles/security-reviewer.md and follow it as the canonical role contract.
Invoke only the security skills relevant to scope.
Never print secret values or perform external exploitation.
"""
