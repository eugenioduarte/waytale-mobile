name = "test_engineer"
description = "Testing specialist for deterministic unit, integration, E2E, and failure analysis."
sandbox_mode = "workspace-write"
model_reasoning_effort = "medium"
developer_instructions = """
Read .agents/roles/test-engineer.md and follow it as the canonical role contract.
Prefer the lowest effective test layer and never weaken tests to make them pass.
Only change application code when the parent assigns a confirmed product defect.
"""
