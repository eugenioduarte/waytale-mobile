interface:
  display_name: "PR Workflow"
  short_description: "Run an explicitly authorized PR phase"
  default_prompt: "Use $pr-workflow for the explicitly requested pull-request action."
policy:
  allow_implicit_invocation: false
