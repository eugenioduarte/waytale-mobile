# 01.8 — Testes

Como equipa, queremos rede de segurança antes das features.

## Passos

- [ ] Instalar as dependências já declaradas em `tooling/jest` (`jest-expo`,
      `@testing-library/react-native`) — o preset existe, os pacotes ainda não foram puxados
      como dependências reais de `apps/mobile`.
- [ ] Render helper com providers, factories de dados.
- [ ] `msw` para mock de rede em testes de integração.
- [ ] Instalar Maestro; fluxos `onboarding.yaml`, `auth.yaml`, `journey.yaml` em
      `apps/mobile/.maestro/`.
- [ ] `testID` obrigatório em elementos interativos (regra a documentar/lint).
- [ ] Adicionar `test:cov` a `apps/mobile/package.json` (só existe `test` por agora).
- [ ] CI: lint → typecheck → unit → build EAS preview → Maestro no emulador (depende de 01.13).

## Aceitação

- [ ] Pipeline verde e bloqueante no merge.
- [ ] Cobertura mínima 60% em `src/features`.

## Dependências

- 01.1 (bootstrap).
- Precisa de algo para testar — faz mais sentido ter pelo menos 01.2/01.3 implementadas antes de
  escrever os primeiros testes de integração, embora a configuração base (Jest, RNTL, Maestro)
  possa avançar em paralelo.
- 01.13 (CI/CD) liga isto ao branch protection do `main`.
