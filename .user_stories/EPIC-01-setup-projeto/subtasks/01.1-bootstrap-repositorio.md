# 01.1 — Bootstrap do repositório

Como dev, quero um projeto Expo com TypeScript estrito e lint/format automáticos, para começar
sem discutir configuração.

## Passos

- [x] Monorepo pnpm workspaces + Turborepo (`pnpm-workspace.yaml`, `turbo.json`).
- [x] `create-expo-app` dentro de `apps/mobile`; `tsconfig` `strict: true` via
      `tooling/typescript`, paths `@/*`.
- [x] ESLint + Prettier partilhados via `tooling/eslint`/`tooling/prettier`.
- [ ] Import/order enforcement (removido do `tooling/eslint/base.js` por conflito de plugin com
      `eslint-config-expo` — resolver ou aceitar que não há regra de ordenação por agora).
- [ ] Husky + lint-staged no pre-commit.
- [ ] Estrutura de pastas completa dentro de `apps/mobile/src/`: `features/<dom>/`, `db/`, `lib/`,
      `stores/`, `i18n/` (hoje só existem `app/`, `components/`, `constants/`, `hooks/` do
      scaffold Expo).
- [ ] `tests/` e `.maestro/` em `apps/mobile/` (pastas ainda não criadas).

## Aceitação

- [x] `pnpm --filter mobile web` gera bundle sem erros (validado com `expo export --platform web`).
- [ ] `pnpm --filter mobile ios` e `pnpm --filter mobile android` arrancam (não testado — precisa
      simulador/dispositivo).
- [x] `pnpm lint`, `pnpm typecheck` passam.
- [ ] `pnpm test` passa (depende de 01.8 — ainda não há testes nem script `test:cov`).
- [ ] Tudo o que precede corre em CI (depende de 01.13).

## Dependências

Nenhuma — é a fundação. Todas as outras subtasks deste épico partem daqui.
