# 01.10 — Estilo com NativeWind (Tailwind)

Como dev, quero utilitários de estilo consistentes, para implementar o design system mais depressa
sem perder os tokens do EPIC-02.

## Passos

- [ ] Instalar NativeWind (pacote) e ligar o `tooling/tailwind/base.js` já existente ao
      `tailwind.config.js` de `apps/mobile` (hoje só há o ficheiro de tokens, falta a instalação
      e o plugin Babel/Metro).
- [ ] Confirmar/definir a cor `accent` — hoje é um placeholder `#000000` em
      `tooling/tailwind/base.js` porque não há hex confirmado (ver TODO no ficheiro); precisa do
      valor real de EPIC-02 antes de ser usada nalguma tela.
- [ ] Documentar a fronteira NativeWind (layout/spacing) vs. componentes do design system
      (semântica de marca) — evitar reinventar variantes de botão em classes soltas.

## Aceitação

- [ ] Uma tela nova consegue ser estilizada só com classes NativeWind, sem `StyleSheet` manual
      para os casos comuns (spacing, cor, tipografia).

## Dependências

- 01.1 (bootstrap).
- Bloqueada pela cor `accent` real, que só existe quando EPIC-02 (design system) estiver definido
  — não usar o placeholder preto em código de produção.
