# 01.5 — Supabase e autenticação

Como dev, quero backend com RLS e sessão persistida, para dados de utilizador seguros.

## Passos

- [x] Criar o projeto Supabase do Waytale (`iqmnbzgsqmmalqzdyxjg`) e ligar o MCP
      (`.mcp.json`, `.codex/config.toml`).
- [ ] Autenticar o MCP nesta máquina: `claude /mcp` num terminal normal (não IDE extension),
      selecionar `supabase` → Authenticate. Por sessão/máquina, não fica no repo.
- [ ] Configurar provider de SMS (Twilio ou similar) no dashboard do Supabase para OTP por
      telefone — feito no dashboard, não precisa de credenciais no código.
- [ ] `apps/mobile/.env.example` com `EXPO_PUBLIC_SUPABASE_URL` / `EXPO_PUBLIC_SUPABASE_ANON_KEY`
      (placeholders) + `.env.local` real (gitignored) com os valores de Settings → API.
- [ ] Cliente Supabase (`src/lib/supabase.ts`) com refresh automático e sessão em SecureStore.
- [ ] Auth por telefone (OTP) + password.
- [ ] RLS por `auth.uid()` em todas as tabelas de utilizador.
- [ ] Storage para áudio e imagens com URLs assinados.
- [ ] Edge Function para geração de rota.

## Aceitação

- [ ] Teste de RLS prova que o utilizador A não lê dados de B.

## Dependências

- 01.1 (bootstrap).
- Bloqueia 01.6 (sync) e a parte "push/analytics" de 01.12 (token FCM registado no Supabase por
  utilizador).
