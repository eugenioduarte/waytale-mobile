# 01.5 — Supabase e autenticação

> **🟡 Implementada e aplicada no projeto — falta o provider SMS** — 2026-09-24
>
> - **Branch:** `0.1-setup` (ainda não merged em `develop`)
> - **Validado:** `pnpm lint`, `pnpm typecheck`, `pnpm test` (mobile 56 + supabase 48 testes),
>   `expo export --platform all` (sem segredos no bundle); revisão `security-reviewer` +
>   `reviewer` (achados corrigidos, ver Notas)
> - **No projeto (verificado com a publishable key):** migrações aplicadas (9 tabelas; sem sessão
>   todas respondem `42501 permission denied`), `generate-route` publicada e a rejeitar pedidos sem
>   JWT de utilizador, registo por email desligado
> - **Pendente (externo):** ligar o login por telefone + Twilio e o captcha no dashboard;
>   autenticar o MCP; rodar as credenciais partilhadas na conversa; ligar os formulários de auth
>   quando o design for importado

Como dev, quero backend com RLS e sessão persistida, para dados de utilizador seguros.

## Passos

- [x] Criar o projeto Supabase do Waytale (`iqmnbzgsqmmalqzdyxjg`) e ligar o MCP
      (`.mcp.json`, `.codex/config.toml`).
- [ ] Autenticar o MCP nesta máquina: `claude /mcp` num terminal normal (não IDE extension),
      selecionar `supabase` → Authenticate. Por sessão/máquina, não fica no repo.
- [ ] Configurar provider de SMS (Twilio ou similar) no dashboard do Supabase para OTP por
      telefone — feito no dashboard, não precisa de credenciais no código. Estado do projeto em
      2026-09-24 (`/auth/v1/settings`): `phone: false` (ainda desligado), `email: false`
      (signup por email já desligado), `phone_autoconfirm: false` (confirmação obrigatória). Falta
      ligar Phone + Twilio e ativar o captcha no OTP.
- [x] Aplicar as migrações no projeto (`supabase link` + `db push`) e publicar `generate-route`
      (`functions deploy`).
- [x] `apps/mobile/.env.example` com `EXPO_PUBLIC_SUPABASE_URL` /
      `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (formato novo `sb_publishable_…`, em vez da anon key
      legacy). O `.env.local` real é criado à mão (gitignored; o agente não tem acesso a `.env*`).
- [x] Cliente Supabase (`src/lib/supabase/client.ts`), criado no primeiro uso, com
      `autoRefreshToken` só em foreground (`AppState`). A sessão fica cifrada: chave AES-256 no
      SecureStore (`AFTER_FIRST_UNLOCK_THIS_DEVICE_ONLY`), sessão AES-GCM no AsyncStorage, com o
      nome da entrada como AAD (`src/lib/supabase/secure-session-storage.ts`, o padrão
      "LargeSecureStore" do Supabase com `expo-crypto` em vez de `aes-js`). No web usa o storage por
      omissão do supabase-js.
- [x] Auth por telefone (OTP) + password — serviço em `src/features/auth/api.ts` (E.164 em
      `phone.ts`, erros mapeados para um vocabulário fixo) e `useSupabaseAuthSync` a espelhar a
      sessão na session store (só o `user.id`, nunca tokens). Um arranque offline mantém a sessão.
      O logout real está no Perfil (`useSignOut`). Os ecrãs de auth continuam placeholders: o
      atalho de preview só existe em `__DEV__` ou sem Supabase configurado.
- [x] RLS por `auth.uid()` em todas as tabelas de utilizador —
      `supabase/migrations/20260923233112_initial_schema.sql`: o schema espelha o SQLite local,
      RLS ligada nas 9 tabelas, `(select auth.uid()) = user_id` com `USING` + `WITH CHECK`,
      `journey_events` só por via da viagem do dono e append-only, catálogo só de leitura,
      `anon` sem acesso e grants explícitos (`auto_expose_new_tables = false`).
- [x] Storage para áudio e imagens com URLs assinados — buckets privados `audio`/`images`
      (`20260923233115_storage_buckets.sql`), leitura só para autenticados e escrita só pelo
      service role; `getSignedMediaUrl` em `src/lib/supabase/media.ts`.
- [x] Edge Function para geração de rota — `supabase/functions/generate-route/`:
      `withSupabase({ auth: 'user' })`, o que põe a query sob a RLS do utilizador. A lógica pura
      (`plan.ts`) valida o pedido e escolhe paragens por distância, interesse e tolerância de
      desvio, dentro do orçamento de tempo. A query só lê `places` dentro da bounding box do raio,
      com `limit`.

## Aceitação

- [x] Teste de RLS prova que o utilizador A não lê dados de B — `supabase/tests/rls.test.ts`
      aplica as migrações num Postgres real (PGlite) com o schema `auth`/`storage` do Supabase e
      corre como `authenticated` com o `sub` de cada utilizador. B não lê, não escreve, não atualiza
      nem apaga dados de A; `anon` não lê nada; todas as tabelas públicas têm RLS. Testado por
      mutação: abrir a policy de `saved_items` faz o teste falhar. No projeto remoto, as mesmas
      migrações estão aplicadas e `anon` recebe `permission denied` em todas as tabelas; o teste
      A/B com utilizadores reais fica para quando o login por telefone estiver ligado.

## Dependências

- 01.1 (bootstrap).
- Bloqueia 01.6 (sync) e a parte "push/analytics" de 01.12 (token FCM registado no Supabase por
  utilizador).

## Notas

- **Supabase CLI:** `pnpm dlx supabase@2.117.0 …` (sem instalação global). Não há Docker nesta
  máquina, por isso o `supabase start`/`test db` local não corre; o PGlite cobre os testes de RLS.
- **`verify_jwt` em `generate-route`:** o `config.toml` tem `true`. No projeto publicado, um
  pedido sem JWT chega à função e é o `withSupabase` que o rejeita (`UNUSABLE_CREDENTIAL`,
  aceita só o modo `user`), por isso a autenticação é garantida na própria função. Confirmar com
  um JWT de utilizador real quando o login por telefone estiver ligado.
- **Achados da revisão, corrigidos:**
  - confirmação SMS obrigatória e signup por email desligado no `config.toml` (sem isso uma conta
    podia ser sequestrada antes de o dono se registar);
  - grants ao `service_role`, que sem auto-exposição ficava sem acesso;
  - arranque offline já não faz logout;
  - uma falha temporária a ler a chave já não apaga a sessão;
  - logout offline conta como sucesso;
  - mais códigos de erro mapeados;
  - query da Edge Function limitada e com bounding box;
  - limites de tamanho em `journey_events`.
- **Para 01.6:**
  - apagar as tabelas de utilizador locais no logout ou na troca de utilizador;
  - os ids do seed de demonstração não são UUID;
  - `sources`/`payload` são texto JSON no SQLite e `jsonb` no Postgres;
  - `stops.place_id` tem `on delete set null` só no Postgres.
- **Rate limiting da Edge Function e captcha no OTP:** por configurar (dashboard / função).
